% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateCopyNumber-fixed.R
\name{calculateCopyNumber_fixed}
\alias{calculateCopyNumber_fixed}
\title{Calculate Copy Number Analysis with Breakpoint-based Arms (Fixed Version)}
\usage{
calculateCopyNumber_fixed(
  segments,
  breakpoints,
  amp_threshold = log2(2.5/2),
  del_threshold = log2(1.5/2),
  stringent_threshold = 0.9,
  output_dir = ".",
  cancer_type = "Unknown",
  use_gistic = TRUE,
  chunk_size = 100,
  save_results = TRUE
)
}
\arguments{
\item{segments}{A data frame containing segmentation data with columns:
\itemize{
\item Sample: Sample identifier
\item Chromosome: Chromosome number (1-22)
\item Start: Segment start position
\item End: Segment end position
\item Log2Ratios: Log2 copy number ratios (or Segment_Mean)
}}

\item{breakpoints}{Breakpoint data, either:
\itemize{
\item Data frame with arm definitions (new format)
\item List with tel_bound element (legacy format)
\item Character string specifying cancer type for built-in data
}}

\item{amp_threshold}{Amplification threshold in log2 scale (default: log2(2.5/2) = 0.32)}

\item{del_threshold}{Deletion threshold in log2 scale (default: log2(1.5/2) = -0.415)}

\item{stringent_threshold}{More stringent threshold for high-confidence calls (default: 0.9)}

\item{output_dir}{Directory for saving results (default: current directory)}

\item{cancer_type}{Cancer type for analysis (used for logging and file naming)}

\item{use_gistic}{Logical, whether to perform GISTIC-style statistical analysis (default: TRUE)}

\item{chunk_size}{Number of samples to process at once for memory efficiency (default: 100)}

\item{save_results}{Logical, whether to save results to files (default: TRUE)}
}
\value{
A list containing:
\itemize{
\item segments: Processed segments with arm annotations
\item arm_summaries: Arm-level copy number summaries
\item gistic_results: GISTIC statistical analysis results (if use_gistic=TRUE)
\item significant_arms: Arms with significant copy number alterations
\item parameters: Analysis parameters used
}
}
\description{
Performs comprehensive copy number analysis using biologically meaningful chromosome
arm breakpoints. Includes proper GISTIC-style statistical analysis and improved
error handling.
}
\examples{
\dontrun{
# Using built-in pan-cancer breakpoints
result <- calculateCopyNumber_fixed(segments, 
                                  breakpoints = "consensus",
                                  cancer_type = "BRCA")

# Using custom breakpoints  
custom_breakpoints <- get_breakpoint_data("breast_invasive_carcinoma")
result <- calculateCopyNumber_fixed(segments, 
                                  breakpoints = custom_breakpoints,
                                  cancer_type = "BRCA")
}

}
