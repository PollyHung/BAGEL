% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-biscut-pipeline.R
\name{run_biscut_batch}
\alias{run_biscut_batch}
\title{Run BISCUT Pipeline on Multiple Cancer Types}
\usage{
run_biscut_batch(
  cancer_folders,
  results_dir,
  cores = 4,
  parallel = FALSE,
  max_parallel = 2
)
}
\arguments{
\item{cancer_folders}{Character vector of cancer folder names}

\item{results_dir}{Character, path to the directory containing cancer-specific results}

\item{cores}{Integer, number of CPU cores to use per cancer type (default: 4)}

\item{parallel}{Logical, whether to run cancer types in parallel (default: FALSE)}

\item{max_parallel}{Integer, maximum number of cancer types to run in parallel (default: 2)}
}
\value{
Named list where each element corresponds to a cancer type and contains
the result from run_biscut_pipeline()
}
\description{
Executes BISCUT analysis on multiple cancer types in parallel or sequentially.
This function provides a convenient way to process multiple cancer datasets
using the same BISCUT pipeline.
}
\examples{
\dontrun{
# Run BISCUT on multiple cancer types
cancer_types <- c("breast_invasive_carcinoma", "lung_adenocarcinoma", "colon_adenocarcinoma")

results <- run_biscut_batch(
  cancer_folders = cancer_types,
  results_dir = "/path/to/cancer_results",
  cores = 4,
  parallel = TRUE,
  max_parallel = 2
)

# Summarize results
successful <- sapply(results, function(x) x$status == "success")
cat("Successful analyses:", sum(successful), "/", length(results), "\n")
}

}
