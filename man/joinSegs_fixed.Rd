% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joinSegs-fixed.R
\name{joinSegs_fixed}
\alias{joinSegs_fixed}
\title{Join Segments Based on Alteration Type (Fixed Version)}
\usage{
joinSegs_fixed(
  segments,
  aneu,
  arm_definition,
  amp_threshold = log2(2.5/2),
  del_threshold = log2(1.5/2),
  telcent = "tel",
  integer = FALSE,
  arm
)
}
\arguments{
\item{segments}{A dataframe containing segment information with columns for \code{Sample},
\code{Chromosome}, \code{Start}, \code{End}, \code{Segment_Mean} (or \code{Log2Ratios}).}

\item{aneu}{A character string indicating the alteration type. Options are "amp", "del",
"non-del", and "non-amp".}

\item{arm_definition}{A data frame or list defining the arm boundaries and coordinates.
If legacy format (list with coord element), will be converted.}

\item{amp_threshold}{Amplification threshold (default: log2(2.5/2))}

\item{del_threshold}{Deletion threshold (default: log2(1.5/2))}

\item{telcent}{A character string indicating whether to check for 'telomere' or 'centromere' alignment.}

\item{integer}{A logical value indicating whether to return integer lengths or normalized percentages.}

\item{arm}{Character string specifying the arm (e.g., "1p", "1q")}
}
\value{
A dataframe summarizing the joined segments with columns for \code{Sample}, \code{percent},
\code{start}, and \code{end}.
}
\description{
This function processes genomic segments based on their alteration type, joining adjacent
segments if the gap between them is smaller than their combined lengths. Includes improved
error handling and cleaner logic.
}
